<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Service label: unique identifier for this agent instance -->
	<!-- INSTALLER: Replace AGENT_ID with the actual agent ID (e.g., molty, bob) -->
	<key>Label</key>
	<string>com.traderbot.agent.AGENT_ID</string>

	<!-- Command to run: traderbot scan in continuous mode -->
	<key>ProgramArguments</key>
	<array>
		<string>/usr/local/bin/traderbot</string>
		<string>scan</string>
		<string>--continuous</string>
	</array>

	<!-- Environment variables: profile token for agent authentication -->
	<!-- INSTALLER: Replace TOKEN_PLACEHOLDER with the actual profile token -->
	<key>EnvironmentVariables</key>
	<dict>
		<key>TRADERBOT_PROFILE_TOKEN</key>
		<string>TOKEN_PLACEHOLDER</string>
	</dict>

	<!-- Working directory: where the agent runs -->
	<!-- INSTALLER: Replace USERNAME with the actual macOS username -->
	<key>WorkingDirectory</key>
	<string>/Users/USERNAME/traderbot</string>

	<!-- Start service automatically when user logs in -->
	<key>RunAtLoad</key>
	<true/>

	<!-- Keep service running: restart on failure, but not on successful exit -->
	<key>KeepAlive</key>
	<dict>
		<key>SuccessfulExit</key>
		<false/>
	</dict>

	<!-- Standard output log -->
	<!-- INSTALLER: Replace USERNAME and AGENT_ID -->
	<key>StandardOutPath</key>
	<string>/Users/USERNAME/Library/Logs/traderbot-AGENT_ID.log</string>

	<!-- Standard error log -->
	<!-- INSTALLER: Replace USERNAME and AGENT_ID -->
	<key>StandardErrorPath</key>
	<string>/Users/USERNAME/Library/Logs/traderbot-AGENT_ID-error.log</string>

	<!-- Process type: background service -->
	<key>ProcessType</key>
	<string>Background</string>

	<!-- Nice value: lower priority than interactive processes -->
	<key>Nice</key>
	<integer>1</integer>
</dict>
</plist>